import React from 'react';
import { Box } from '@mui/material';

const ContactNavbar: React.FunctionComponent = () => {
	return <Box id='some' sx={{ display: 'flex' }}></Box>;
};

export default ContactNavbar;
